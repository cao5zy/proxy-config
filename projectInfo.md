# proxy-config 

## 1. 项目概述

proxy-config是一个用于管理微应用的工具，它能够：
- 自动发现微应用（支持多个扫描目录）
- 构建微应用的Docker镜像
- 生成nginx反向代理配置
- 生成docker-compose.yml文件
- 管理容器的生命周期（启动、停止、清理）
- 支持微应用的预构建和后清理脚本
- 管理微应用之间的网络通信
- 生成网络地址列表用于连通性排查
- **通过nginx作为统一入口，所有微应用通过反向代理访问，不直接暴露端口到主机**
