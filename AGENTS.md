# micro_proxy — 开发约定

## 核心理念：纯函数与副作用分离

本项目的架构核心原则是**严格区分纯函数与副作用**。这既是设计原则，也是测试策略的基础。

```
┌─────────────────────────────────────────────────┐
│  纯函数层（Pure Layer）                           │
│  - 输入输出确定，无文件/网络/Docker 调用           │
│  - 可推理、可组合、可单元测试                       │
│  - **TDD 强制作用域**                              │
├─────────────────────────────────────────────────┤
│  副作用边界（Side-effect Boundary）               │
│  - 薄薄的胶水层，调纯函数 + 做 I/O                 │
│  - 不包含业务逻辑，只编排调用顺序                    │
│  - 不要求单元测试（可做集成测试）                   │
└─────────────────────────────────────────────────┘
```

### 设计时的体现

**新增功能时，先把逻辑写成纯函数，再在副作用边界中调用它。**

```rust
// ✅ 好的做法：纯函数 + 薄胶水
fn compute_image_name(app_name: &str) -> String {
    format!("{}:latest", app_name)
}

fn build_and_tag(app_name: &str) -> Result<()> {
    let image = compute_image_name(app_name);
    Command::new("docker").args(["build", "-t", &image]).status()?;
    Ok(())
}
```

```rust
// ❌ 不好的做法：逻辑混在副作用中
fn build_and_tag(app_name: &str) -> Result<()> {
    let image = format!("{}:latest", app_name);  // 逻辑和 I/O 混在一起
    Command::new("docker").args(["build", "-t", &image]).status()?;
    Ok(())
}
```

这样设计的原因：
- **纯函数可以 TDD**：你可以在不启动 Docker 的情况下先写测试
- **副作用边界没有业务逻辑**：不值得为它写单元测试，集成测试就够了
- **职责清晰**：新人一看就知道哪里找规则、哪里找流程

### 对已有代码的态度

不要求一次性重构整个代码库。但在**每次修改涉及的文件中**，如果遇到逻辑混在副作用里的代码，应当：
1. 先（在测试的保护下）将纯逻辑提取为纯函数
2. 在纯函数上应用 TDD 完成改动
3. 副作用胶水层只做编排

---

## 测试策略

### 什么需要 TDD（红-绿-重构）

| 需要 TDD         | 不需要单元测试                  |
| -------------- | ------------------------ |
| 配置解析逻辑         | Docker 命令调用              |
| Nginx 配置生成     | Shell 脚本执行               |
| 名称/路径推导        | 容器状态查询                   |
| 验证函数（validate） | 网络创建/删除                  |
| 序列化/反序列化       | 文件写入/读取（除 TempDir 隔离的测试） |
| 状态计算           | main/cli 入口编排            |

如果一行代码必须依赖外部系统才能验证其正确性，那它不属于 TDD 的作用域。

### Tier 分级

| 改动规模 | 测试要求 |
|----------|----------|
| **Tier 1：单行/微小改动**（变量重命名、注释修正、日志追加等） | 不需新建测试。运行 `cargo test` 确认不破坏已有测试即可。 |
| **Tier 2：添加纯函数或修改纯函数逻辑** | 必须先写测试（TDD 红-绿-重构），再实现。 |
| **Tier 3：添加/修改副作用边界**（编排函数、CLI 命令、Docker 交互） | 不要求单元测试。逻辑部分必须提取为纯函数按 Tier 2 处理。 |

### TDD 工作流

对于纯函数部分，严格执行红-绿-重构：

1. **红**：先写一个会失败的测试，描述期望的行为
2. **绿**：写刚好能让测试通过的最小实现
3. **重构**：在测试的保护下优化代码质量

```
[写测试] → [测试失败] → [写最少代码] → [测试通过] → [重构] → [提交]
    ↑                                                        │
    └────────────────────────────────────────────────────────┘
```

### 测试约定

#### 位置

- **纯函数单元测试**：放在被测试模块末尾的 `#[cfg(test)] mod tests { ... }` 中
- **集成测试**（副作用边界的端到端验证）：放在 `tests/` 目录下

#### 风格

- 遵循项目已有的测试风格（见 `src/config.rs`、`src/nginx.rs` 的 `#[cfg(test)]`）
- 使用 `tempfile::TempDir` 做文件系统隔离，不污染真实目录
- 测试中使用 `use super::*` 引入被测模块的纯函数。如果被测函数不是 `pub`，通过 `crate::` 路径引入
- 使用 `assert_eq!`、`assert!`、`assert!(result.is_err())` 等标准断言宏
- 对边界条件和错误路径都编写测试，不仅测试"快乐路径"
- 测试命名用 `snake_case`，描述被测试的方法名和场景，例如 `test_validate_empty_scan_dirs`、`test_generate_location_config_static_root`

#### 辅助模式

```rust
// 文件 I/O 测试使用 tempfile
use tempfile::TempDir;

#[test]
fn test_config_save_and_load() {
    let tmp = TempDir::new().unwrap();
    let path = tmp.path().join("test.yml");

    // ... 测试逻辑

    // TempDir 在作用域结束时自动清理
}
```

## 运行测试

```bash
# 运行全部测试
cargo test

# 只运行某个模块的测试（开发时最常用）
cargo test <module_name>

# 运行单个测试
cargo test <test_name>

# 显示日志输出
cargo test -- --nocapture

# 运行所有测试并显示全部输出
cargo test 2>&1
```

## Bug 修复流程

1. 先判断 bug 在纯函数层还是副作用边界
2. **纯函数层**：先写一个能复现 bug 的测试（红），再修复代码（绿）
3. **副作用边界**：追到调用的纯函数，在那层加测试修复；如果胶水层有编排错误，直接修
4. 运行 `cargo test` 确认没有回归

## 质量检查清单

在标记为完成之前，确认：

- [ ] `cargo test` 全部通过
- [ ] 新增的纯函数有对应的 TDD 测试
- [ ] 没有把应该提取为纯函数的逻辑留在副作用边界中
- [ ] 测试覆盖了边界条件和错误路径
- [ ] 没有注释掉或 `#[ignore]` 的测试（除非有明确理由）
- [ ] 没有删除或修改已有测试的行为
